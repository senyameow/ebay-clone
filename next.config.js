/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'crobux.ru',
            'lh3.googleusercontent.com'
        ]
    }
}

module.exports = nextConfig
